[
  {
    "task": "Parse an AWS S3 bucket name from a full S3 URI (s3://bucket-name/key/path) and extract just the bucket name",
    "format": "regex"
  },
  {
    "task": "Create a JSON configuration object for an AWS Lambda function that includes runtime, handler, timeout, and memory allocation settings",
    "format": "json"
  },
  {
    "task": "Write a Python function that takes an AWS CloudWatch log message and extracts the timestamp in ISO 8601 format",
    "format": "python"
  }
]